{
  "name": "server-health-monitoring-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && CI=false npm run build",
    "start": "cd backend && node server.js"
  }
}
