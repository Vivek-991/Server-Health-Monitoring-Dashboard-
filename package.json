{
  "name": "server-health-monitoring-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install && cd ../frontend && npm install",
    "build": "npm run postinstall && cd frontend && CI=false npm run build",
    "start": "cd backend && node server.js"
  }
}
