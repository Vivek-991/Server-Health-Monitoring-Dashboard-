{
  "name": "server-health-monitoring-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Server Health Monitoring Dashboard Monorepo Root",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix backend",
    "start": "npm start --prefix backend",
    "dev": "npm start --prefix backend"
  }
}
